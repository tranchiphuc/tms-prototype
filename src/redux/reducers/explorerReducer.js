const initialState = {
  queryResult: [],
  rawPayload: [],
  loading: false,
};

export default function explorerReducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
